<!DOCTYPE html>
<html>
  <head>
    <title>Verification Pages</title>

    <%= javascript_pack_tag 'verification_tool' %>
  </head>

  <body>
    <div id="verification-tool">
      <p>{{status}}</p>
      <p>Statement is about: <span v-if="statements.length > 0">{{ statements[0].person_name }}</span></p>
    </div>
  </body>
</html>
